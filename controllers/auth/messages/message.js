const messageInput = require('./messageInput');
const messagePassword = require('./messagePassword');
const messageError = require('./messageError');

module.exports = {
  messageInput,
  messagePassword,
  messageError
};
